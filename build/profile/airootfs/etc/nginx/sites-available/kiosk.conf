server {
    listen 80 default_server;
    listen [::]:80 default_server;

    root /var/www/kiosk;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location /logo.png {
        alias /etc/easy-kiosk/logo.png;
    }
}
